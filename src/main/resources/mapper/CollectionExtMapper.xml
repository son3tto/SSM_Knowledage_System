<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tree.community.mapper.CollectionExtMapper">
  <resultMap id="BaseResultMap" type="com.tree.community.model.Collection">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Sun Mar 29 23:25:11 CST 2020.
    -->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="user_id" jdbcType="BIGINT" property="userId" />
    <result column="question_id" jdbcType="BIGINT" property="questionId" />
    <result column="folder_id" jdbcType="BIGINT" property="folderId" />
    <result column="gmt_create" jdbcType="BIGINT" property="gmtCreate" />
  </resultMap>

  <select id="getCollectionCount" parameterType="java.lang.Long" resultType="java.lang.Integer">
    SELECT count(*) FROM (SELECT count(*) FROM collection where question_id = #{id} group by user_id) t;
  </select>

  <insert id="addCollection" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="id">
    insert into collection (user_id, question_id, folder_id, gmt_create)
    values
    <foreach collection="list" item="item" index="index" separator=",">
      (
      #{item.userId},
      #{item.questionId},
      #{item.folderId},
      #{item.gmtCreate}
      )
    </foreach>
  </insert>

  <delete id="deleteCollection">
    delete from collection
    where user_id = #{userId} and question_id = #{questionId} and folder_id IN
    <foreach collection="folderIds" item="item" index="index" open="(" close=")" separator=",">
      #{item}
    </foreach>
  </delete>
</mapper>